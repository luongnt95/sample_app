<% if object.errors.any? %>
	<div class="error_explaination">
		<div class="alert alert-danger">This form contains <%= pluralize(object.errors.count, "error") %></div>
		<ul>
			<% object.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>